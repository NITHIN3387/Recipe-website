export default function Logo(prop) {
    return(
        <div>
            <span style={{color: '#fb9201', fontSize: `${prop.size}em`}}>eat</span>            
            <span style={{color: '#6f4eeb', fontSize: `${prop.size}em`}}>dish_.</span>            
        </div>
    )
}